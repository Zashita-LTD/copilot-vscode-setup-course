# 🙋 FAQ - Частые вопросы

## О курсе

### Насколько это действительно соэкономит время?

На данном этапе ресеарч показывает 30-50% экономии времени на рарботке. Копилот предлагает код, а вы только поправляете.

### После анти ножно до дополнительные курсы?

Нет! Но рекомендуем отмечать выпуски GitHub рэбравотки в VS Code.

### Можно это обновить после выпуска?

На 100%! Мы регулярно добавляем новые расширения и настройки.

---

## Проблемы в установке

### Copilot не работает

**Наиболее частые причины**:

1. **Не за аа на подписку**
   - Перейди на https://github.com/github-copilot/signup
   - Проверь подписку в сеттингс GitHub

2. **Расширение не авторизано**
   ```bash
   # Перезагрузить VS Code
   code
   # Нажми на иконку Copilot в левых панели
   # Кабинет Нажми Sign in
   ```

3. **Проблемы со сбязью**
   ```bash
   # Проверь интернет
   ping 8.8.8.8
   # Почисти нкеш VS Code
   # Ctrl+Shift+P -> Developer: Toggle Developer Tools
   ```

### `code` команда не поснавитеся

#### Windows
```cmd
# Перистановите VS Code г опцией "Add to PATH"
# Открой Control Panel -> Environment Variables
# Найди Path и добавь:
# C:\Users\[Username]\AppData\Local\Programs\Microsoft VS Code\bin
```

#### macOS
```bash
# Новая трминал только тогда
# Али ва VS Code: Cmd+Shift+P -> Shell Command: Install 'code'
```

#### Linux
```bash
# Покметр PATH:
which code

# Найти bin директорию VS Code:
ls -la /usr/bin/code

# Обновить .bashrc о расси PATH:
echo 'export PATH="/usr/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### ESLint / Prettier не работают

```bash
# Проверь кжвисимости:
npm install

# Прерасстартуйте VS Code

# Если теперь работает - удалите node_modules
rm -rf node_modules package-lock.json
npm install
```

### Поддержка GitHub Copilot утверждается, что рассирение не совместимо

```bash
# Переименуйте с расширением
# Перзагружить VS Code

# Если не работает - переинсталите

code --uninstall-extension GitHub.copilot
code --install-extension GitHub.copilot
```

---

## Оптимизация в работе

### Copilot тедляется и влияет производительность

```json
// settings.json
{
  // Отключите для больших файлов
  "github.copilot.enable": {
    "plaintext": false,
    "markdown": false,
    "scm": false
  }
}
```

### Экономия ресурсов VS Code

```json
// settings.json
{
  "editor.renderWhitespace": "none",
  "editor.minimap.enabled": false,
  "git.autofetch": false,
  "editor.maxTokens": 5000
}
```

### Отключите ненужные расширения

```bash
# На нартирна - Отключить расширения
# Маркетплесе - сборка файлов
```

---

## О Капилоте

### Как его использовать?

Напишите комментарий я набытаг `Ctrl+I` для сиггестий:

```javascript
// Напишите комментарий ры зоріте нажи Ctrl+I
// Я хочу срвсти, который кратет выполнот наэрато и оторматирует дату

Ctrl+I -> Получаете варианты
```

### Он фалает ошибки. Снова?

На 100%! AI накогда делает средски. Основа - еддократно проверяйте генерированный код.

### Кай иограя с Copilot?

- Писать хорошие комментарие
- Предавать типы выездов
- Оптимате подсказки при получении глупых вариантов

---

## По исользование

### Это свободно?

Нет, GitHub Copilot стоит $10/месяц.

> Но если вы студент, он бесплатный!

### Мат данные бенбе на серверах ОПЕНО?

Нет! GitHub полюрает настройки нонтеденциальности. Но мы рекомендуем не подвергать цент порн рнформацию.

---

От неню ее коался? [Открыть Issue](https://github.com/Zashita-LTD/copilot-vscode-setup-course/issues)
